# 開発ログ 2026-08-14

## [2026-08-14 16:18] 対話記録
プロンプト:
"""
先に要件定義書を作ろうか
"""

回答・提供コード:
v2刷新にあたり、これまでの会話で確定した方針を要件定義書として `SPEC.md`（ルート直下）に新規作成した。CLAUDE.md の仕様書テンプレートに準拠。

主な確定事項:
- 目的: PC作業中の強制反復学習 ＋ 就活ポートフォリオ（フルスタック学習）
- スコープ: Mac常駐アプリ継承 / ローカルファースト(SQLite)＋Supabase同期 / Webダッシュボード(Tier1優先) / 単語追加auto-fill(和訳・品詞) / LLM刷新(Gemini主・gemma従・ローカル生成保険)
- 対象外: スマホ受験(Tier2)・AWS・発音記号・Google Sheets
- 技術: Python3.12/rumps/PyObjC, SQLite, Supabase(Tokyo), Next.js/Vercel, Gemini, uv
- データ: 3テーブル(words / learning_records / answer_log)、UUID・updated_at・deleted・イベントソーシング
- 未確定(TBD)6項目を末尾に列挙（出題間隔・認証方式・例文生成タイミング・可視化指標・ローカルLLMモデル・複数意味対応）

状態: **進行中**（ドラフト作成のみ。未確定事項の確定待ち。動作確認対象外の文書のため実装DoDは非該当）

補足: 直前に旧コード一式を `legacy/` へ退避し、root を空にして v2 を開始する準備を実施済み。

## [2026-08-14 16:2x] 対話記録
プロンプト:
"""
学習目的ってことは要件定義に入れなくていいよ
"""

回答・提供コード:
SPEC.md 1.1 目的から「就活ポートフォリオ／フルスタック学習教材」の記述を削除し、プロダクト自体の要件（PC作業中の強制反復学習）のみに絞った。学習・就活の意図は要件定義には含めない方針を確認。

状態: **完了**（該当箇所の削除を確認済み）

## [2026-08-14 16:3x] 対話記録
プロンプト:
"""
全てOk
"""

回答・提供コード:
提示した未確定6点すべてに承認を得たため、SPEC.md を確定版に更新した。

確定した設計判断（新設「11. 確定した設計判断」）:
| 項目 | 決定 |
|------|------|
| 出題間隔デフォルト | 5分（QUIZ_INTERVAL_MINUTES で変更可） |
| 認証方式 | Googleログイン（Supabase Auth）。Macは service_role で認証不要 |
| 例文の生成タイミング | 不正解時に生成しキャッシュ。登録auto-fillは和訳・品詞のみ |
| ローカルLLMフォールバック | gemma 2B（実測で不足なら7B） |
| auto-fillの意味数 | 主要1件のみ（1行=1つの(英語,和訳)ペア） |

Webダッシュボード表示指標（Tier1・確定4種）:
1. 正答率の推移（日別・時系列）← answer_log
2. 継続日数 streak（カレンダーヒートマップ）← answer_log
3. 苦手単語 Top（誤答率ランキング）← answer_log × words
4. 復習予定数（今日/今週）← learning_records.next_review

あわせて実施:
- 更新履歴に2件追記（降順）
- 9章の「将来: 入力式やdirection切替を検討・TBD」を末尾TBD参照に整合
- 残TBDを3件に整理（Mac起動方法 / Vercelデプロイ手順 / 4択の当てずっぽう対策の要否）

状態: **完了**（SPEC.md の記載内容を確認済み。文書のため実行検証は非該当）

## [2026-08-14 16:4x] 対話記録
プロンプト:
"""
もちろんAで進めるよ
"""

回答・提供コード:
初期設計書 `design-docs/20260814-initial.md` を作成。あわせて CLAUDE.md 必須フォルダ `design-docs/` `docs/` `src/` を作成した。

設計書の内容（全10章）:
0. 背景と方針 — v1の問題5件と対応表、中核4原則（ローカルファースト / イベントソーシング / 同期は裏方 / 秘密情報をクライアントに置かない）
1. フォルダ構成 — CLAUDE.md 準拠で Mac常駐アプリは `app/` ではなく `src/` に配置。`src/{db,sync,llm,srs}/` に分割
2. DB設計 — SQLite DDL（WAL・外部キー・インデックス）とPostgresとの差分。ローカル専用列 `synced_at` / `synced` を定義し、answer_log の synced=0 自体を送信キューとする（別テーブル不要）
3. データアクセス層 `store.py` の API 一覧とスレッド安全性方針（単一接続 + Lock + WAL）
4. 同期設計 — Push/Pull手順、テーブル別の衝突解決方針（words=LWW / answer_log=衝突なし）
5. LLM層 — Gemini → gemma 2B → ローカル生成 の3段。③が必ず成功するため例外を投げない設計。v1の `_extract_example_line` / `_sentence_uses_word` は再利用
6. Mac UI — v1の良い点（LSUIElement・非ブロッキングパネル・1秒自動クローズ）を維持し、4点を変更（統計復元 / 閉じるボタンのみ / 即時保存 / メニュー追加）
7. Webアプリ構成
8. 実装順序 Phase1〜6（各DoD付き）。Phase2終了時点でオフライン完結アプリが完成する計画
9. v1課題とPhaseの対応表
10. 実装時に判断する事項3件（SM-2のquality値 / gemma品質 / 同期間隔）

DBファイル配置: `~/Library/Application Support/VocabLib/vocablib.db`

状態: **完了**（設計書作成。文書のため実行検証は非該当。実装は未着手）
