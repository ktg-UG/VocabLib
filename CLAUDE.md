# CLAUDE.md - プロジェクトガイドライン

## 🚨 セッション開始時の必須動作

**ユーザーとの対話を開始する前に、必ず以下を実行すること：**

1. プロジェクトルートに以下のフォルダ・ファイルが存在するか確認する
   - `src/`
   - `docs/`
   - `development-logs/`
   - `design-docs/`（設計書を入れるフォルダ）
   - `SPEC.md`（ルート直下の仕様書）
2. 存在しないフォルダ・ファイルがあれば、即座に作成する
3. `README.md` がルート直下に存在しなければ、最低限のテンプレートで作成する

```bash
mkdir -p src docs development-logs design-docs
touch SPEC.md README.md
```

この動作は**ユーザーから何も指示されていなくても、セッションの最初に必ず実行**すること。これによりチーム全体で開発環境を統一する。

---

## 🛑 作業前ルール（推測禁止）

**コードを書く前に、必ず以下を守ること：**

- **仕様で迷ったら必ず開発者に確認する。推測で実装しない。**
  - 「たぶんこういう意図だろう」で進めない
  - 不明点は箇条書きで一度に質問する
- **スコープ外のファイルは触らない。**
  - 依頼された変更と関係ないファイルは編集しない
  - リファクタや改善提案がしたい場合は、別タスクとして提案するだけにとどめる
- **新しいライブラリを入れる前に開発者に確認する。**

---

## ✅ 完了の定義（DoD: Definition of Done）

「完了」と呼べるのは、**動作確認まで終わった状態のみ**。

| 状態                     | 扱い             |
| ------------------------ | ---------------- |
| コードを書いただけ       | **進行中** |
| ビルド・実行を通した     | **進行中** |
| 想定通りに動くか確認した | **完了**   |

- 動作確認をしていないものは「完了」と書かない
- 動作確認の結果（成功 / 失敗 / 未確認）を必ずdevlogに記録する

---

## ⚠️ エラー・詰まった時のルール

- **エラーは隠さず即報告。**勝手に試行錯誤を繰り返してdevlogを汚さない。
- **devlogにエラー全文を記録する。**省略や要約はしない。
  - エラーメッセージ全文
  - 発生時のコマンド or 操作
  - スタックトレースがあれば全部

---

## 📒 開発ログ記録ルール（必須要件）

### 🔗 業務報告gemとの連携

このプロジェクトのdevlogは、**業務報告生成gemの入力データ**として使われる。
gemはdevlogを読み取って LINE / Slack / Google Docs の3チャネル分の業務報告を生成する。
したがってdevlogは「自分用のメモ」ではなく「業務報告の素材」として書くこと。

- 何をやったか／なぜやったか／結果どうなったか をセットで書く
- 数値・期限・依頼事項は具体的に書く
- 推測や曖昧表現を書かない（gemが拾ってしまう）

### 記録形式

各回答時に以下の形式でログを記録すること：

```
[日付と時刻] 対話記録
プロンプト:
"""
(受信したプロンプトをそのまま記録)
"""

回答・提供コード:
(説明と生成したコード全て)

エラー報告を受けた場合は追加で：
エラー内容: (エラーメッセージ)
```

### 重要事項

- **1対話 = 1記録。ユーザーからのプロンプトを受け取って回答するたびに、必ず1ブロック追記すること**（数回まとめて書かない）
- **些細なやり取りも含めて漏れなく記録すること**（「了解しました」だけのやり取りもスキップしない）
- **時系列で全記録を保存すること**（後から書き直さない）
- **毎回このファイル(CLAUDE.md)を確認して実行すること**
- **日付ごとに新しいログファイルを作成すること**
- **ログファイルは `development-logs/` 配下に配置し、ファイル名は `YYYYMMDD-devlogs.md` 形式で統一すること**（例: `20260602-devlogs.md`）
- **エラー報告**: エラーメッセージは必ず記録に含める

---

## 🌿 Gitルール

- **コミットはなるべく細かく切る。**1コミット = 1つの意味のある変更単位。
  - 1日分の作業を1コミットにまとめない
  - 動作確認できた単位でコミットする
- **コミットメッセージは何をしたか分かるように書く。**
  - 例：`feat: ログイン画面のバリデーション追加`
  - 例：`fix: フォーム送信時のNullエラー修正`
  - 例：`docs: SPEC.md にAPI仕様を追記`
- **動かない状態でコミットしない。**最低限ビルドが通る状態で切る。

---

## 📁 ファイル構成

### フォルダ構造テンプレート（必須）

プロジェクトは以下の構造で管理すること。役割ごとに必ずフォルダを分けること。

```
プロジェクトルート/
├── README.md            ← プロジェクト概要
├── CLAUDE.md            ← 本ガイドライン
├── SPEC.md              ← 仕様書（プロジェクト全体の確定事項）
├── design-docs/         ← 設計書（機能・変更ごと。AIへの指示書）
├── src/                 ← ソースコード
├── docs/                ← クライアント提供資料など
└── development-logs/    ← 開発ログ（YYYYMMDD-devlogs.md）
```

- **src/**: ソースコード一式（Python, GAS など）
- **docs/**: クライアントから受領した資料、参考ドキュメント、外部仕様書などを格納（自分たちの仕様書は `SPEC.md` に書く）
- **development-logs/**: 開発ログを格納。ファイル名は `YYYYMMDD-devlogs.md`（記録形式は本ファイル「開発ログ記録ルール」を参照）
- **design-docs/**: 設計書を格納。機能ごと・変更ごとに1ファイル（書き方は「設計書ルール」を参照）
- **SPEC.md**: 本プロジェクトの仕様書。ルート直下に配置（命名は `SPEC.md` で固定）
- **README.md**: ルート直下に配置

---

## 📘 README.md ルール

README.mdには以下の3項目を **必ず** 書く。これ未満は許容しない。

### 1. プロジェクト概要

- 何のためのプロジェクトか（1〜2文）
- 主な機能（箇条書き3〜5項目）

### 2. セットアップ手順

- 必要な前提（言語バージョン、ライブラリ等）
- インストール手順（コマンドを順番に）
- 環境変数の説明（`.env.example` を用意）

### 3. 実行方法

- 起動コマンド
- 動作確認URL（あれば）
- 主要なエンドポイント or 画面遷移（あれば）

---

## 📐 設計書 と 📄 仕様書 の違い（必読）

混同しやすいので明確に分ける。**役割もライフサイクルも別物**。

|                          | 設計書                                  | 仕様書（SPEC.md）                   |
| ------------------------ | --------------------------------------- | ----------------------------------- |
| **役割**           | AI Agent にコードを書かせるための指示書 | プロジェクト全体の確定事項まとめ    |
| **書く内容**       | 何を / どうやって作るか                 | 何が決まっているか                  |
| **作成タイミング** | 機能追加・コード変更のたび              | プロジェクト開始時 + 仕様確定のたび |
| **単位**           | 機能ごと・変更ごとに1ファイル           | プロジェクト全体で1ファイル         |
| **置き場所**       | `design-docs/`                        | ルート直下`SPEC.md`               |
| **ライフサイクル** | 機能完成後はアーカイブ扱い              | 常に最新の確定状態を保つ            |

### 🔁 開発フロー

```
1. 設計書を書く（design-docs/{機能名}.md）
       ↓
2. AI Agent に設計書を渡してコードを生成・変更
       ↓
3. 動作確認（DoDを満たすか）
       ↓
4. 確定した内容を SPEC.md に反映（更新履歴に追記）
       ↓
5. devlogに作業記録を残す
```

**設計書は作業の起点、仕様書は確定事項の終着点。** 設計書だけ更新してSPEC.mdを放置しない。

---

## 📐 設計書ルール（必須）

### 設計書を書くタイミング

- **プロジェクトの初期設計のとき**
- 新機能を追加するとき
- 既存機能を大きく変更するとき
- 1ファイル内の小修正でも、AIに渡す前に「何を／どうやって」が言語化できていないなら書く

### ファイル名（日付プレフィックス必須）

- `design-docs/YYYYMMDD-{機能名}.md` の形式で統一する
  - 例: `design-docs/20260611-login.md`
  - 例: `design-docs/20260615-csv-import.md`
- 初期設計は `design-docs/YYYYMMDD-initial.md`
- 同じ機能を改修する場合も新しい日付で別ファイルを作る（過去分は上書きしない）
  - 例: `design-docs/20260611-login.md` → `design-docs/20260620-login.md`

### 書き方

**フォーマットは決めない。好きなように書いてよい。**
「何を作るか」「どうやって作るか」がAIに伝わればOK。箇条書きでも文章でも図でも自由。

ただし **書いた日と対象機能** だけは必ず分かるようにする（ファイル名で日付・機能が判別できればOK。本文冒頭に再掲してもよい）。

### 重要事項

- **設計書なしでAIにコードを書かせない**。書かせる前に必ず1枚作る。
- 完了したら **必ず SPEC.md に反映** する（反映しない設計書は知識の蓄積にならない）。
- 設計書は破棄しない。`design-docs/` に履歴として残す。

---

## 📄 仕様書ルール（必須）

### 仕様書とは

- **プロジェクト全体の決定事項をまとめた1ファイル**（`SPEC.md`）
- 「現時点で何がどう動くべきか」が常に最新の状態で書かれている
- 設計書で実装した内容が確定したらここに反映する
- 「これから作りたいこと（願望）」は書かない。**確定したことだけ**書く。

### 書くべきこと（原則）

- **確定事項を書く**：仕様未定の項目は書かない（「TBD」として残す）
- **具体値で書く**：「適切な間隔」ではなく「5分ごと」、「主要ユーザー」ではなく「営業部の田中さん・佐藤さん」
- **読み手は数日後の自分／引継ぎ相手**：明日の自分が忘れていても再現できる粒度で
- **設計書と重複しない**：設計書は「どう作るか（指示書）」、SPEC.mdは「何がどう動くか（事実）」

### ルール

- **仕様書はプロジェクトルート直下の `SPEC.md` に統一すること**（フォルダに分けたり別名にしたりしない）
- **ファイル名は必ず `SPEC.md`（Markdown形式）とすること**
- **仕様が変わるたびに開発者に確認をとってから `SPEC.md` を更新すること**
- **冒頭に「更新履歴」セクションを設け、いつ・何を更新したかを表で記録すること**

### 仕様書テンプレート

必須セクション（どの案件でも書く）と、案件によって追加するセクションに分けている。
使わないセクションは削除してOK。

```markdown
# {プロジェクト名} 仕様書

## 更新履歴
| 日付 | 更新内容 | 担当 |
|------|----------|------|
| yyyy-mm-dd | {変更内容を具体的に} | {氏名} |
| yyyy-mm-dd | 初版作成 | {氏名} |

---

## 1. 概要
### 1.1 目的（なぜ作るか）
（クライアントが解決したい課題を1〜2文で）

### 1.2 解決する課題
（現状の困りごと → どう解決するか）

### 1.3 スコープ
- やること:
  - 〇〇
- やらないこと（明示）:
  - 〇〇

---

## 2. 関係者
| 役割 | 氏名 / 会社 | 連絡先 |
|------|-------------|--------|
| クライアント担当 | 〇〇さん（〇〇社） | 〇〇 |
| 開発担当 | 〇〇 | 〇〇 |

---

## 3. 環境・技術スタック
| 区分 | 内容 |
|------|------|
| 言語 | Python 3.11 / GAS など |
| 主要ライブラリ | pandas 2.x, openpyxl 3.x |
| 実行環境 | ローカル / GCP / GAS など |
| データ保存先 | Google Sheets / SQLite / 〇〇 |
| デプロイ先 | 〇〇 |

---

## 4. 機能仕様
### 4.1 機能一覧
| ID | 機能名 | 概要 | ステータス |
|----|--------|------|-----------|
| F-01 | 〇〇 | 〇〇 | 実装済 / 開発中 / 未着手 |

### 4.2 各機能の詳細
#### F-01 {機能名}
- 入力: 〇〇
- 処理: 〇〇
- 出力: 〇〇
- 例外時の挙動: 〇〇

---

## 5. データ仕様
### 5.1 データ構造
（テーブル / Sheet / JSONスキーマ等。図でもOK）

### 5.2 主要項目
| 項目名 | 型 | 必須 | 説明 |
|--------|----|----|------|
| user_id | str | ○ | 〇〇 |

---

## 6. 外部連携 ※あれば
| 連携先 | 用途 | 認証方式 |
|--------|------|----------|
| Google Sheets API | データ保存 | サービスアカウント |

---

## 7. 環境変数・設定値
| 変数名 | 用途 | 例 / 取得先 |
|--------|------|------------|
| API_KEY | 〇〇 | .env に記載 |

---

## 8. 運用
### 8.1 起動・実行方法
（コマンド / トリガー / 手順）

### 8.2 定期実行 ※あれば
（cron / GASトリガー設定）

### 8.3 ログ・監視 ※あれば
（どこに出るか、何を監視するか）

---

## 9. 制約・既知の問題 ※あれば
- 〇〇は△△制限により対応不可
- 〇〇のときエラーになる（対応予定: 〇〇）

---

## 10. 用語集 ※あれば
| 用語 | 説明 |
|------|------|
| 〇〇 | 〇〇 |
```

### 重要事項

- **更新履歴は最新が上になるよう降順で記載すること**
- **「更新内容」は変更箇所が分かるよう具体的に書くこと**（例: 「API仕様変更」ではなく「ユーザー登録APIのレスポンス形式を変更」）
- **仕様変更時に更新履歴の追記を忘れないこと**
- **設計書の内容を実装完了したら、必ずSPEC.mdに反映してから「完了」とすること**
- **未確定項目は `TBD` と書いて残す**（空欄で流さない）
- **使わないセクションは削除してOK**（雛形をそのまま残さない）
